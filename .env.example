# Ephemera AI Environment Configuration Template
# Copy this file to .env and fill in your actual values

# Logging Configuration
RUST_LOG=debug

# LLM Configuration (OpenAI-compatible API required)
# Model must support function calling for tool usage
# Examples: deepseek-chat, gpt-5
BASE_URL=<llm-model-provider-base-url>
API_KEY=<your-api-key-here>
MODEL_NAME=<model-name>

# Embedding Model Configuration

# Embedding API Configuration
# For OpenAI-compatible embedding APIs
# Note: Do NOT include /embeddings in the URL - Rig adds it automatically
EMBEDDING_MODEL_URL=<embedding-model-provider-base-url>
EMBEDDING_MODEL_API_KEY=<your-embedding-api-key>
EMBEDDING_MODEL=<embedding-model-name>

# Embedding Dimensions Configuration (REQUIRED)
# Number of dimensions for embedding vectors - must match your embedding model
EMBEDDING_MODEL_DIMENSIONS=2048

# Database Configuration
# MySQL database for structured memory metadata
EPHA_MEMORY_MYSQL_URL=mysql://epha:123456@localhost:3306/epha_memory

# Vector Database Configuration
# Qdrant for semantic search and embeddings
EPHA_MEMORY_QDRANT_URL=http://localhost:6334

# Dialogue Atrium Database Configuration
# MySQL database for dialogue atrium
DIALOGUE_ATRIUM_MYSQL_URL=mysql://epha:123456@localhost:3307/dialogue_atrium

# Security Notes:
# - Never commit your .env file to version control
# - Keep your API keys secure and private
# - Use different keys for development and production